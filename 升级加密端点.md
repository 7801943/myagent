# 升级加密端点实施计划

## 目标

新增一个“私有加密隧道”作为现有 HTTP/WS 的平行入口。现有 FastAPI 路由、静态资源、文件上传下载、WebSocket 业务协议保持不变；私有客户端内置 LocalProxy，WebView 只访问本机 `127.0.0.1`，LocalProxy 与服务端私有端口之间传输自定义加密帧。

该方案不引入 Nginx、TLS/mTLS、CA、证书续期流程。第一版目标是避免网络链路上传输明文，不承诺防客户端逆向；设备绑定、密钥轮换、混淆、吊销等第二层加固仅预留接口。

## 架构

```text
私有客户端 WebView
    |
    | http://127.0.0.1:<local_port>
    | ws://127.0.0.1:<local_port>/ws
    v
客户端 LocalProxy
    |
    | MyAgent Private Tunnel v1，加密 TCP 帧
    v
服务端 PrivateTunnelServer :9443
    |
    | 127.0.0.1:8000 明文回环转发
    v
现有 FastAPI / Uvicorn
```

服务端已经新增 `myagent.interfaces.web.private_tunnel`。启用后，它监听 `private_transport.listen_port`，每个私有连接完成握手后连接 `private_transport.upstream_host:upstream_port`，然后做双向字节流转发。HTTP、WebSocket Upgrade、文件下载、`StreamingResponse` 都作为普通 TCP 字节流穿过隧道。

### OnlyOffice 链路

客户端使用 FastAPI 的同源 `/onlyoffice` 反向代理。浏览器加载的 `api.js`、编辑器资源以及协作 WebSocket 都先进入 LocalProxy，再通过同一个 `private_transport.listen_port` 到达 FastAPI：

```text
WebView /onlyoffice/*
    -> 客户端 LocalProxy
    -> PrivateTunnelServer :9443
    -> FastAPI /onlyoffice
    -> OnlyOffice DocumentServer
```

因此客户端应让 `documents.onlyoffice_url` 保持为 `/onlyoffice`。FastAPI 到 DocumentServer 的地址由 `documents.onlyoffice_internal_url` 独立配置。

## 服务端配置

`config.yaml` 新增：

```yaml
private_transport:
  enabled: true
  listen_host: "0.0.0.0"
  listen_port: 9443
  upstream_host: "127.0.0.1"
  upstream_port: 8000
  protocol_version: 1
  server_key_id: "server-v1"
  server_private_key: "${MYAGENT_PRIVATE_SERVER_KEY}"
  client_psk_id: "client-v1"
  client_psk: "${MYAGENT_PRIVATE_CLIENT_PSK}"
  max_frame_size: 1048576
  handshake_timeout_seconds: 10
  idle_timeout_seconds: 300
```

依赖：

```toml
cryptography>=42.0
```

生成服务端 X25519 私钥和客户端内置公钥：

```bash
python - <<'PY'
import base64
from cryptography.hazmat.primitives.asymmetric import x25519
from cryptography.hazmat.primitives import serialization

key = x25519.X25519PrivateKey.generate()
private_raw = key.private_bytes(
    encoding=serialization.Encoding.Raw,
    format=serialization.PrivateFormat.Raw,
    encryption_algorithm=serialization.NoEncryption(),
)
public_raw = key.public_key().public_bytes(
    encoding=serialization.Encoding.Raw,
    format=serialization.PublicFormat.Raw,
)
enc = lambda b: base64.urlsafe_b64encode(b).decode().rstrip("=")
print("MYAGENT_PRIVATE_SERVER_KEY=" + enc(private_raw))
print("CLIENT_EMBED_SERVER_PUBLIC_KEY=" + enc(public_raw))
PY
```

`client_psk` 建议使用至少 32 字节随机值；普通字符串按 UTF-8 使用，`base64:<value>` 前缀按 base64url 解码。

## Private Protocol V1

### 基础约定

- 承载：TCP。
- 一个私有隧道连接承载一个 WebView 本地 TCP 连接。
- 第一版不做多路复用。
- JSON 握手消息使用 `uint32_be length || canonical_json_utf8`。
- canonical JSON：UTF-8、按 key 排序、无额外空格，等价 Python `json.dumps(..., sort_keys=True, separators=(",", ":"))`。
- 二进制字段使用 base64url，无 `=` padding。
- 加密套件：`X25519 + HKDF-SHA256 + ChaCha20-Poly1305`。

### ClientHello

客户端生成临时 X25519 keypair 和 16 字节随机数，发送：

```json
{
  "type": "client_hello",
  "magic": "MAGT",
  "version": 1,
  "suite_id": 1,
  "suite": "X25519-HKDF-SHA256-CHACHA20POLY1305",
  "server_key_id": "server-v1",
  "client_psk_id": "client-v1",
  "client_nonce": "<base64url 16 bytes>",
  "client_ephemeral_public": "<base64url 32 bytes>"
}
```

服务端校验：

- `magic == "MAGT"`
- `version == 1`
- `suite_id == 1`
- `server_key_id` 匹配当前服务端 key id
- `client_psk_id` 匹配当前 PSK id

### ServerHello

服务端生成临时 X25519 keypair 和 16 字节随机数，先构造不含 `server_finished` 的 `server_core`：

```json
{
  "type": "server_hello",
  "magic": "MAGT",
  "version": 1,
  "suite_id": 1,
  "suite": "X25519-HKDF-SHA256-CHACHA20POLY1305",
  "server_key_id": "server-v1",
  "client_psk_id": "client-v1",
  "client_nonce": "<echo client_nonce>",
  "server_nonce": "<base64url 16 bytes>",
  "server_ephemeral_public": "<base64url 32 bytes>"
}
```

双方计算：

```text
dh_static    = X25519(client_ephemeral_private, server_static_public)
dh_ephemeral = X25519(client_ephemeral_private, server_ephemeral_public)

handshake_secret = HKDF-SHA256(
  ikm    = dh_static || dh_ephemeral || client_psk,
  salt   = SHA256(raw_client_hello_payload || raw_server_core_payload),
  info   = "myagent-private-v1 handshake",
  length = 32
)
```

服务端视角等价为：

```text
dh_static    = X25519(server_static_private, client_ephemeral_public)
dh_ephemeral = X25519(server_ephemeral_private, client_ephemeral_public)
```

服务端计算：

```text
server_finished = HMAC-SHA256(
  key = handshake_secret,
  msg = "server finished" || SHA256(raw_client_hello_payload || raw_server_core_payload)
)
```

实际发送的 `ServerHello` 是 `server_core` 加上：

```json
{
  "server_finished": "<base64url 32 bytes>"
}
```

客户端必须重建 canonical `server_core`，验证 `server_finished`，再进入下一步。

### ClientFinished

客户端发送：

```json
{
  "type": "client_finished",
  "client_finished": "<base64url 32 bytes>"
}
```

其中：

```text
client_finished = HMAC-SHA256(
  key = handshake_secret,
  msg = "client finished" || SHA256(raw_client_hello_payload || raw_server_hello_payload)
)
```

服务端校验失败必须立即关闭连接。

### 传输密钥

握手完成后，双方计算：

```text
traffic_secret = HKDF-SHA256(
  ikm    = handshake_secret,
  salt   = SHA256(raw_client_hello_payload || raw_server_hello_payload || raw_client_finished_payload),
  info   = "myagent-private-v1 traffic",
  length = 32
)

client_to_server_key = HKDF-SHA256(traffic_secret, salt=None, info="c2s key",   length=32)
server_to_client_key = HKDF-SHA256(traffic_secret, salt=None, info="s2c key",   length=32)
c2s_nonce_prefix     = HKDF-SHA256(traffic_secret, salt=None, info="c2s nonce", length=4)
s2c_nonce_prefix     = HKDF-SHA256(traffic_secret, salt=None, info="s2c nonce", length=4)
```

### 加密帧

每个加密帧：

```text
uint32_be frame_len
uint8     frame_type
bytes     ciphertext_with_16_byte_tag
```

`frame_len = 1 + len(ciphertext_with_16_byte_tag)`。

`frame_type`：

```text
0x01 DATA
0x02 PING
0x03 PONG
0x04 CLOSE
0x05 ERROR
```

Nonce：

```text
nonce = 4-byte direction_nonce_prefix || uint64_be(sequence_number)
```

规则：

- 每个方向独立 `sequence_number`，从 0 开始。
- 同一个 key 下 nonce 绝不能复用。
- `ChaCha20-Poly1305` associated data：

```text
"MAGT" || uint16_be(1) || uint8(frame_type) || uint64_be(sequence_number) || uint32_be(ciphertext_len)
```

其中 `ciphertext_len` 是 `ciphertext_with_16_byte_tag` 的长度。

DATA 明文就是原始 TCP bytes。PING/PONG 明文可为空，也可带少量回显 payload。CLOSE 表示对端正常结束。任何 AEAD 校验失败、未知帧类型、超大帧都必须关闭连接。

默认单帧明文上限 `1048576` 字节。超过上限的本地 TCP 数据必须拆成多个 DATA 帧。

## 客户端适配要求

客户端编码 agent 需要实现 LocalProxy：

1. 启动本地 HTTP/TCP 代理，监听 `127.0.0.1:<random_port>`。
2. WebView 打开 `http://127.0.0.1:<random_port>/`。
3. 对每个 WebView 到 LocalProxy 的 TCP 连接，创建一个到服务端 `server:9443` 的 TCP 连接。
4. 按 Private Protocol V1 完成握手。
5. 将 WebView 发来的原始 TCP bytes 拆成 DATA 帧，加密后发给服务端。
6. 将服务端 DATA 帧解密后写回 WebView TCP 连接。
7. WebView 侧无需知道加密协议，现有 `fetch('/api/...')`、文件下载、WebSocket 都应自然工作。

客户端需内置：

```text
server_public_key
server_key_id
client_psk
client_psk_id
protocol_version = 1
```

## 预留加固接口

第一版暂不实现，但协议和配置需预留以下概念：

```text
client_id
device_id
client_key_version
server_key_id
client_psk_id
min_client_version
revoked_client_ids
revoked_key_ids
attestation_payload
obfuscation_profile
```

后续可扩展：

- 客户端设备绑定。
- 客户端静态公钥 allowlist。
- 客户端密钥分版本轮换。
- 服务端按 `client_id` 限流。
- 协议头混淆。
- 私有客户端二进制混淆。
- 服务端动态下发新 key id。

## 测试清单

服务端：

- `private_transport.enabled=false` 时现有服务行为不变。
- 正确 PSK 与正确 server public key 可完成握手。
- 错误 PSK 握手失败。
- 错误 `server_key_id` 或 `client_psk_id` 握手失败。
- 篡改任意加密帧后连接关闭。
- 大文件下载能完整通过隧道。
- `/ws` 能连接、心跳、收发消息。
- 多个并发 HTTP 请求可分别建立隧道连接并正常完成。

客户端联调：

- WebView 可加载首页静态资源。
- 登录、登出、`/api/auth/me` 正常。
- session 列表、chat WebSocket 正常。
- workspace 文件上传、下载正常。
- OnlyOffice 的 `api.js`、文档加载、保存和协作 WebSocket 均通过主隧道正常工作。
- 私有隧道断开后 LocalProxy 能向 WebView 返回连接失败，并可重新建链。
